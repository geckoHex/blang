Language: blang (Beck's Language)
Purpose: very basic and simple scripting language to learn c++

Commands:

None yet.

<!-- - print(<string>) -->

Syntax:

1. Create a file ending in `.blang`
2. Write each command. Separate each with a semicolon.

Compile and run the interpreter:

1. g++ blang.cpp -o build/blang
2. ./build/blang